<% unless current_user?(@user) %>
  <div id="follow_form">
    <% if current_user.following?(@user) %>
        <p>Already Follow</p>
    <%else%>
      <%= render 'follow' %>
    <%end%>
  </div>
<%end%>
